[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai_assistant"
version = "0.1.0"
description = "Minimal AI assistant package"
authors = [ { name = "You" } ]
readme = "README.md"
requires-python = ">=3.11"
